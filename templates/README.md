Templates used to create release artifacts in CI by replacing `REPLACE_VERSION` string.
- charts - helm charts that are deployed to ghcr.io/fraillt as OCI images
- static - static manifests with default values deployed as release artifacts
